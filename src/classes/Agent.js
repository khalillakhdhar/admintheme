export class Agent {
  constructor(id, nom, prenom, email, mdp, telephone, adresse) {
    this.id = id
    this.nom = nom
    this.prenom = prenom
    this.email = email
    this.mdp = mdp
    this.telephone = telephone
    this.adresse = adresse
  }
}
